# Rainfall Visual Studio Language Extension

This incluedes the syntax definition, to get syntax highlighting. Install with:

```
$ npm install vsce
$ node_modules/.bin/vsce package
$ code --install-extension ./rainfall-vscode-XX.XX.XX.vsix
```
